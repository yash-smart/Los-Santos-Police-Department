<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Los Santos Police Department</title>
</head>
<body>
    <h1>Los Santos Police Department</h1>
    <% for (let i=0;i<data.length;i++) { %>
        <a href="/news-update/<%= data[i].id %>/<%= user_id %>"><p><%= data[i].text %></p></a>
    <% } %>
    <% if (admin) { %>
        <a href="/news-announcements-add/<%= user_id %>"><button>Add</button></a>
    <% } %>
</body>
</html>